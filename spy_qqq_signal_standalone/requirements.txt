# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Scheduler
APScheduler==3.10.4

# Data + ML (pin major versions to match the research environment)
numpy>=1.26,<2.0
pandas>=2.1,<3.0
pyarrow>=14.0
scikit-learn>=1.4,<2.0
scipy>=1.12
joblib>=1.3          # model serialisation (ships with scikit-learn, explicit pin)

# Market data + HTTP
requests>=2.31
yfinance>=0.2.50

# HTML table parsing (for Multpl CAPE scrape)
lxml>=5.0
html5lib>=1.1

# Plotting (imported by analysis tools)
matplotlib>=3.8
