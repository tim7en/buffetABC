# Copy this file to .env and fill in real values.
# .env is git-ignored — never commit secrets.

# Bearer token required to call POST /refresh
# Leave empty to make the endpoint open (local dev only!)
REFRESH_TOKEN=change-me-in-production

# How many hours before the signal is considered stale (default 25)
MAX_STATE_AGE_H=25
